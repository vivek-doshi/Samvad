# nginx config — proxy /api/* to backend:8000
# Note 1: This file is mounted into the Angular frontend Nginx container.
# It serves static Angular files and proxies API requests to the FastAPI backend.
# See infra/nginx/nginx.conf for the full annotated configuration template.
# TODO: Add nginx configuration
